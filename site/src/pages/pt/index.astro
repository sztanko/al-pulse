---
/* Thin wrapper. The page's content lives in a view so that the English and
   Portuguese routes are the same code with a different `lang`, rather than two
   copies drifting apart. English keeps the URLs it already has; Portuguese
   sits under /pt. */
import Home from '../../views/Home.astro';
---

<Home lang="pt" />
