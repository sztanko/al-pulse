---
/* The method page is long-form prose, so it is one file per language rather
   than a page driven by dictionary keys. Two pages of dense explanation broken
   into fifty lookups would be unreadable to maintain, and translations of
   running text need to be written as running text, not assembled from
   fragments. */
import MethodPt from '../../views/MethodPt.astro';
---

<MethodPt />
